@page "/dashboard"
@using CulinaryCommand.Components.Custom

<PageTitle>Dashboard</PageTitle>

<div class="container mt-4">
    <div class="row g-5">
        <div class="col-12 col-md-6">
            <DashboardCard Title="Prep Items Needing Attention" Value="5 items" Color="#ffc107" IconClass="bi bi-exclamation-triangle" Link="/prep" />
            <DashboardCard Title="Orders Pending Approval" Value="3 orders" Color="#688f72" IconClass="bi bi-file-earmark-check" Link="/orders" />
            <DashboardCard Title="Daily Summaries / Reports" Value="1" Color="#883ae1" IconClass="bi bi-file-text" Link="/reports" />
        </div>
        <div class="col-12 col-md-6">
            <DashboardCard Title="Active Tasks by Employee" Value="6 tasks" Color="#3662e3" IconClass="bi bi-people" Link="/tasks" />
            <DashboardCard Title="Low Stock Ingredients" Value="2" Color="#ca3a31" IconClass="bi bi-box-seam" Link="/inventory" />
            <DashboardCard Title="Manage Employees" Value="35" Color="#e28743" IconClass="bi bi-person-gear" Link="/employees" />
        </div>
    </div>

    <div class="row">
        <div class="card shadow-sm d-flex flex-row align-items-center px-4 py-4 mb-5">
            <div class=col-12>
                <p class="">
                    Recent Activity
                </p>
                
                <div class="col-12 flex-row justify-items-center">
                    d
                </div>
                
                <ActivityCard>
                    
                </ActivityCard>
            </div>
        </div>
    </div>
</div>

@code {

}